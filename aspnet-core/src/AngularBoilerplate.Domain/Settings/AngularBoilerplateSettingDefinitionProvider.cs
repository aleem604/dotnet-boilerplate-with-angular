using Volo.Abp.Settings;

namespace AngularBoilerplate.Settings;

public class AngularBoilerplateSettingDefinitionProvider : SettingDefinitionProvider
{
    public override void Define(ISettingDefinitionContext context)
    {
        //Define your own settings here. Example:
        //context.Add(new SettingDefinition(AngularBoilerplateSettings.MySetting1));
    }
}
